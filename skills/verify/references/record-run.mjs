// ~/claude-skills/skills/verify/references/record-run.mjs
// Shim: the implementation lives in the router. Node resolves this file's real path, so the relative import holds through the ~/.claude/skills symlink.
import '../../../router/record-run.mjs';
